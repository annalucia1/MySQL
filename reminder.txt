select a,b;
need comma between them

or need to start with variables + option
city like ‘’ or city like ‘’;

distinct can be regarded as an attachment to the variable


order BY a, b;

average is AVG
round floor 
what the diff between ceil and round?

print space-separated values
